[package]
name = "raft"
version = "0.1.0"
edition = "2024"

[dependencies]
prost = "0.14.3"
proto = { path = "../proto" }
thiserror = "2.0.18"
tracing = "0.1.44"
rand = "0.10.1"

[target.'cfg(target_arch = "wasm32")'.dependencies]
getrandom = { version = "0.4", features = ["wasm_js"] }
